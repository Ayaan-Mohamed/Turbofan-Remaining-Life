# Turbofan-Remaining-Life
This project builds a machine learning classifier to predict imminent engine failure using the NASA C-MAPSS turbofan dataset. The model analyzes multisensor telemetry from operating engines to identify those approaching the end of their useful life so maintenance can be scheduled proactively rather than reactively.
