data/              # Don't commit the dataset folder
*.csv              # Don't commit any CSV files you create
__pycache__/       # Python's compiled cache — generated automatically
.ipynb_checkpoints # Jupyter's internal state — regenerates automatically
.venv/             # Your virtual environment — not portable anyway
.vscode/           # VSCode settings — personal to your machine